<% provide(:title, 'Sign up') %>
<% provide(:button_text, 'Create my account') %>
<h1 class="p-fifty">Sign up</h1>
<div class="row">
  <div class="col-md-6 col-md-offset-3">
    <%= render 'form_user' %>
    <span>
      <%= render "devise/shared/links" %>
     </span>
  </div>
</div>
